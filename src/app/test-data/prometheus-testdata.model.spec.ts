import { PrometheusTestdata } from './prometheus-testdata.model';

describe('PrometheusTestdata', () => {
  it('should create an instance', () => {
    expect(new PrometheusTestdata()).toBeTruthy();
  });
});
