import { KClusterMachineDeployment } from './kcluster-machine-deployment.model';

describe('KClusterMachineDeployment', () => {
  it('should create an instance', () => {
    expect(new KClusterMachineDeployment()).toBeTruthy();
  });
});
