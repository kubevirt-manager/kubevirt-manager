import { LoadBalancerPort } from './load-balancer-port.model';

describe('LoadBalancerPort', () => {
  it('should create an instance', () => {
    expect(new LoadBalancerPort()).toBeTruthy();
  });
});
