import { KubeVirtClusterInstanceType } from "./kube-virt-clusterinstancetype";


describe('KubeVirtClusterInstanceType', () => {
  it('should create an instance', () => {
    expect(new KubeVirtClusterInstanceType()).toBeTruthy();
  });
});
