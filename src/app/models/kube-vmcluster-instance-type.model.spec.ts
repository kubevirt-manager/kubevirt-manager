import { KubeVMClusterInstanceType } from './kube-vmcluster-instance-type.model';

describe('KubeVMClusterInstanceType', () => {
  it('should create an instance', () => {
    expect(new KubeVMClusterInstanceType()).toBeTruthy();
  });
});
